# Integers are objects.

# Python uses arbitrary-precision integers.
# Integers in python doesn't overflow because it automatically allocates more memory. Capacity grows when needed.

# Python integers can grow as large as memory allows.

# Intger Caching (Interning)_
# Python performs an optimization for commonly used small integers.

# x = 42
# print(x.bit_length()) - shows how many bits are used.

# Converting Between Bases:
# bin(value) - shows binary
# hex(value) - shows hex
# oct(value) - shows octal
